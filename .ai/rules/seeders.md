---
paths:
  - 'database/seeders/**'
---

# Seeders

## Keep florist demo data opt-in
FloristDemoSeeder is development-only, targets the active company named Mi Florería, and must never be added to DatabaseSeeder. Stock openings and recipes must continue to use the official inventory and catalog Actions; never write stock_balances or recipe tables directly.
